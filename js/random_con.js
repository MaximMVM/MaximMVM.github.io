function getRandomInt(min, max) {
  return Math.floor(Math.random() * (max - min)) + min;
}

var mm = x.length;

x[random1]; из основного масива
x[random2];
x[random3];

str.split(/ /)[random2]; слово из массива

